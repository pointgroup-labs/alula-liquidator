# Generated by Cargo
# will have compiled files and executables
debug/
target/

# These are backup files generated by rustfmt
**/*.rs.bk

# MSVC Windows builds of rustc generate these, which store debugging information
*.pdb

# OS ignore rules
.DS_Store
.AppleDouble
.LSOverride
[Dd]esktop.ini
Thumbs.db
Thumbs.db:encryptable
ehthumbs.db
ehthumbs_vista.db
*.stackdump
*.cab
*.msi
*.msix
*.msm
*.msp
.Trash-*
*~

# IDEs
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea

## soroban/Rust output
target

# dotenv environment variable files
.env
.env.*
# Tracked: the committed template that operators copy to `.env`.
!.env.example

.claude
CLAUDE.md

tmp
# Local SQLite DB lives at repo root (`data.db`, `data-*.db`, …).
# Anchored to root so deploy/grafana/provisioning/datasources/ etc.
# aren't swept up.
/data*

config.json